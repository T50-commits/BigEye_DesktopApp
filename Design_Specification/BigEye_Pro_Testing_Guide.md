# BigEye Pro — คู่มือทดสอบระบบก่อนเปิดให้บริการ
### สำหรับคนเดียว คอมเครื่องเดียว ไม่ต้องเขียนโค้ด
### Date: February 2026

---

## หลักการ

คุณไม่ต้องเขียน test script เอง แค่ทำตามขั้นตอนนี้ทีละข้อ เหมือน checklist
ทดสอบบนเครื่องเดียวได้ โดยรัน server + client บนเครื่องเดียวกัน

---

## ขั้นตอน 0: เตรียมสภาพแวดล้อม

### 0.1 รัน Server (Backend) บนเครื่องตัวเอง

```bash
cd BigEye_Desktop_App/server
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
```

เปิดไว้ตลอดการทดสอบ (อย่าปิด terminal นี้)

### 0.2 เปลี่ยน Client ให้ชี้ไป localhost

เปิดไฟล์ `client/core/config.py` แก้:
```python
BACKEND_URL = "http://localhost:8000"  # ← ใช้ตอนทดสอบ
# BACKEND_URL = "https://api.bigeye.pro"  # ← ใช้ตอน production
```

### 0.3 เปิด Firestore Emulator (ฟรี ไม่เสียเงิน)

```bash
# ติดตั้ง Firebase CLI (ครั้งเดียว)
npm install -g firebase-tools
firebase login

# เปิด Emulator
cd BigEye_Desktop_App/server
firebase emulators:start --only firestore
```

**ถ้ายุ่งยากเกินไป:** ใช้ Firestore จริงก็ได้ แค่สร้าง project แยกชื่อ `bigeye-pro-test`
ข้อมูลทดสอบลบทิ้งทีหลังได้

### 0.4 สร้างโฟลเดอร์ทดสอบ

สร้างโฟลเดอร์บน Desktop ชื่อ `TestFiles/` ใส่:
- รูป 5 ไฟล์ (.jpg)
- วิดีโอ 2 ไฟล์ (.mp4)
- รูป 1 ไฟล์ที่เสีย (เปลี่ยนนามสกุล .txt → .jpg) — สำหรับทดสอบ error

---

## ทดสอบ Phase 1: Authentication (สมัคร + เข้าสู่ระบบ)

### Test 1.1 — สมัครสมาชิก
| ขั้นตอน | ทำอะไร | คาดหวัง |
|:-:|:--|:--|
| 1 | เปิดโปรแกรม BigEye Pro | เห็นหน้า Sign In / Register |
| 2 | กด Register tab | เห็นฟอร์ม: Name, Email, Phone, Password, Confirm |
| 3 | กรอก: Test User, test@test.com, 0812345678, password123 | — |
| 4 | กด "Create Account" | ✅ เข้าสู่หน้าหลัก เห็น "Test User" มุมขวาบน |
| 5 | ปิดโปรแกรม แล้วเปิดใหม่ | ✅ เข้าหน้าหลักได้เลย ไม่ต้อง login ใหม่ (token saved) |

### Test 1.2 — Login ผิดรหัส
| ขั้นตอน | ทำอะไร | คาดหวัง |
|:-:|:--|:--|
| 1 | Logout ก่อน | กลับหน้า Sign In |
| 2 | ใส่ email ถูก + password ผิด | ✅ เห็น "Incorrect email or password" (สีแดง) |
| 3 | ใส่ email ที่ไม่มี | ✅ เห็น error message (ไม่ crash) |

### Test 1.3 — สมัครซ้ำ
| ขั้นตอน | ทำอะไร | คาดหวัง |
|:-:|:--|:--|
| 1 | กด Register → ใส่ test@test.com อีกครั้ง | ✅ เห็น "This email is already registered" |

### Test 1.4 — Device Mismatch (จำลอง)
วิธีจำลอง: ไปแก้ hardware_id ใน Firestore ของ user เป็นค่าอื่น แล้ว login
| ขั้นตอน | ทำอะไร | คาดหวัง |
|:-:|:--|:--|
| 1 | แก้ hardware_id ใน Firestore | — |
| 2 | Login ด้วย email/password ที่ถูก | ✅ เห็น "Device mismatch" error |
| 3 | แก้ hardware_id กลับ | Login ได้ปกติ |

**ผ่าน Phase 1:** ✅ สมัคร/เข้าสู่ระบบ/error ทำงานถูกต้อง

---

## ทดสอบ Phase 2: Credit System (เติมเงิน + ยอดเงิน)

### Test 2.1 — ดูยอดเครดิต
| ขั้นตอน | ทำอะไร | คาดหวัง |
|:-:|:--|:--|
| 1 | Login สำเร็จ | เห็น "💰 0 credits" ใน Top Bar |
| 2 | กดปุ่ม Refresh (↻) | ยอดเงินไม่เปลี่ยน (ยังเป็น 0) |

### Test 2.2 — เติมเงิน (จำลอง)

เนื่องจาก slip verification ต้องใช้ API จริง ให้**เพิ่มเครดิตโดยตรงใน Firestore**:

```
ไปที่ Firebase Console → Firestore → users/{user_id}
แก้ credits: 0 → 1000
```

| ขั้นตอน | ทำอะไร | คาดหวัง |
|:-:|:--|:--|
| 1 | แก้ credits เป็น 1000 ใน Firestore | — |
| 2 | กด Refresh (↻) ในโปรแกรม | ✅ เห็น "💰 1,000 credits" |

### Test 2.3 — TopUp Dialog
| ขั้นตอน | ทำอะไร | คาดหวัง |
|:-:|:--|:--|
| 1 | กด "Top Up" | ✅ เห็น TopUp dialog พร้อมข้อมูลธนาคาร |
| 2 | ลาก/เลือกรูปสลิป + ใส่จำนวน | ✅ กดส่งได้ (อาจ error ถ้า slip API ยังไม่พร้อม — OK) |

### Test 2.4 — History
| ขั้นตอน | ทำอะไร | คาดหวัง |
|:-:|:--|:--|
| 1 | กด "History" | ✅ เห็น dialog ตาราง (อาจว่างเปล่าก็ได้) |

**ผ่าน Phase 2:** ✅ ยอดเงินแสดงถูก, refresh ทำงาน, dialog เปิดได้

---

## ทดสอบ Phase 3: AI Processing — Happy Path (สำคัญที่สุด)

### Test 3.1 — iStock Mode, รูป 5 ไฟล์

**ก่อนทดสอบ:** ตรวจสอบว่า
- มีเครดิตอย่างน้อย 50 ใน Firestore
- มี Gemini API Key (ไปเอาจาก https://aistudio.google.com/apikey)

| ขั้นตอน | ทำอะไร | คาดหวัง |
|:-:|:--|:--|
| 1 | ใส่ Gemini API Key → กด Save | ✅ key ถูกบันทึก |
| 2 | เลือก Model: gemini-2.5-flash (ถูกกว่า ทดสอบเร็ว) | — |
| 3 | เลือก Platform: iStock | Keyword Style ซ่อน |
| 4 | ตั้ง Keywords: 45, Title: 70, Description: 200 | — |
| 5 | กด Open Folder → เลือก TestFiles/ | ✅ เห็น thumbnails 7 ไฟล์ + stats "📸5 🎬2" |
| 6 | ดู Cost Estimate Bar | ✅ "📁 7 files · ≈ 21 credits · (iStock × 3) · ✓ Sufficient" |
| 7 | กด START | ✅ ConfirmDialog popup: "7 files, 21 credits, ..." |
| 8 | กด Start ใน dialog | ✅ ปุ่มเปลี่ยนเป็น STOP (สีแดง) |
| 9 | รอ... (30-120 วินาที) | ✅ เห็น progress bar เพิ่มขึ้น, thumbnails เปลี่ยนสี |
| 10 | เสร็จ! | ✅ SummaryDialog แสดง: Successful, Failed, Credits, CSV paths |
| 11 | ปิด SummaryDialog | ✅ กลับหน้าหลัก, thumbnails มี ✓ เขียว |
| 12 | คลิกรูปที่สำเร็จ | ✅ Inspector แสดง title, description, keywords |
| 13 | ดู CSV files ที่สร้าง | ✅ เปิดใน Excel/Numbers ได้ ข้อมูลครบ |

### Test 3.2 — ตรวจสอบคุณภาพ CSV

| เช็คอะไร | วิธี | คาดหวัง |
|:--|:--|:--|
| Title ยาวถูก | นับตัวอักษร | 50-70 ตัวอักษร |
| Description ไม่ตัดกลางประโยค | อ่านดู | จบด้วย "." |
| Keywords เป็น Dictionary words | สุ่มเช็ค 10 คำ | ทุกคำอยู่ใน Dictionary.txt |
| ไม่มีคำซ้ำ | ดูใน Excel | ไม่มี keyword ซ้ำกัน |
| column ครบ | นับ column | ตรงตาม config.py |

### Test 3.3 — Adobe & Shutterstock Mode

| ขั้นตอน | ทำอะไร | คาดหวัง |
|:-:|:--|:--|
| 1 | เปลี่ยน Platform: Adobe & Shutterstock | ✅ Keyword Style ปรากฏ |
| 2 | เลือก Keyword Style: Hybrid | — |
| 3 | กด START → ทำ 3 รูป | ✅ สร้าง CSV 2 ไฟล์: Adobe + Shutterstock |
| 4 | เปิด Adobe CSV | ✅ columns: Filename, Title, Keywords, Category, Releases |
| 5 | เปิด Shutterstock CSV | ✅ columns: Filename, Description, Keywords, ... |

**ผ่าน Phase 3:** ✅ AI ทำงาน → CSV ถูกต้อง → ข้อมูลมีคุณภาพ

---

## ทดสอบ Phase 4: Error Handling (สิ่งที่ผิดพลาด)

### Test 4.1 — เครดิตไม่พอ
| ขั้นตอน | ทำอะไร | คาดหวัง |
|:-:|:--|:--|
| 1 | แก้เครดิตใน Firestore เป็น 5 | — |
| 2 | Refresh → เลือก 7 files (ต้องใช้ 21 cr) | — |
| 3 | ดู Cost Bar | ✅ "✕ Insufficient" สีแดง |
| 4 | กด START | ✅ InsufficientDialog: "Required: 21 / Available: 5 / Shortfall: 16" |
| 5 | กด "Process 1 file" | ✅ ทำได้ 1 ไฟล์ |

### Test 4.2 — ไม่มี API Key
| ขั้นตอน | ทำอะไร | คาดหวัง |
|:-:|:--|:--|
| 1 | กด Clear ที่ API Key | — |
| 2 | กด START | ✅ Error: "Please enter Gemini API Key" (ไม่ crash) |

### Test 4.3 — API Key ผิด
| ขั้นตอน | ทำอะไร | คาดหวัง |
|:-:|:--|:--|
| 1 | ใส่ API Key: "abc123wrong" → Save | — |
| 2 | กด START → ประมวลผล | ✅ ไฟล์ทุกตัว error (icon แดง ✕) |
| 3 | คลิกไฟล์ที่ error | ✅ Inspector แสดง error message |
| 4 | ดู SummaryDialog | ✅ Successful: 0, Failed: 7, Refunded: 21 credits |

### Test 4.4 — ไฟล์เสีย
| ขั้นตอน | ทำอะไร | คาดหวัง |
|:-:|:--|:--|
| 1 | เตรียมรูปเสีย 1 ไฟล์ (.txt เปลี่ยนเป็น .jpg) ใน TestFiles/ | — |
| 2 | เปิด folder → START | ✅ ไฟล์เสีย error, ไฟล์อื่นสำเร็จ, refund ถูกต้อง |

### Test 4.5 — กด STOP กลางทาง
| ขั้นตอน | ทำอะไร | คาดหวัง |
|:-:|:--|:--|
| 1 | เปิด folder ที่มี 7 files → START | — |
| 2 | รอจนทำไป 2-3 ไฟล์ → กด STOP | ✅ หยุดทำงาน |
| 3 | ดู SummaryDialog | ✅ แสดงจำนวนที่ทำได้ + refund ส่วนที่ยังไม่ทำ |
| 4 | เช็คเครดิตใน Firestore | ✅ ถูก refund จำนวนที่ไม่ได้ทำ |

### Test 4.6 — Server ปิด (Maintenance)
| ขั้นตอน | ทำอะไร | คาดหวัง |
|:-:|:--|:--|
| 1 | ปิด terminal ที่รัน server (Ctrl+C) | — |
| 2 | กด Refresh ในโปรแกรม | ✅ เห็น error (ไม่ crash) |
| 3 | กด START | ✅ เห็น error "Cannot connect to server" |
| 4 | เปิด server ใหม่ | ✅ กด Refresh → ยอดเงินกลับมา |

**ผ่าน Phase 4:** ✅ ทุก error มี message ชัดเจน ไม่ crash

---

## ทดสอบ Phase 5: Crash Recovery (ทดสอบสำคัญมาก)

### Test 5.1 — จำลอง crash กลางทาง
| ขั้นตอน | ทำอะไร | คาดหวัง |
|:-:|:--|:--|
| 1 | เปิด folder 7 files → START | — |
| 2 | รอจน processing 3/7 | — |
| 3 | **Force quit โปรแกรม** (Cmd+Q บน Mac / Alt+F4 บน Windows) | โปรแกรมปิดทันที |
| 4 | เช็คไฟล์ `~/.bigeye/recovery.json` | ✅ มีข้อมูล job ที่ค้าง |
| 5 | เปิดโปรแกรมใหม่ | ✅ RecoveryDialog: "Unfinished job found" |
| 6 | กด OK | ✅ แสดง refund จำนวนเครดิต |
| 7 | เช็คเครดิตใน Firestore | ✅ ถูก refund ส่วนที่ไม่ได้ทำ |
| 8 | เช็คไฟล์ recovery.json | ✅ ถูกลบแล้ว |

**ผ่าน Phase 5:** ✅ ปิดกลางทาง → เปิดใหม่ → refund อัตโนมัติ

---

## ทดสอบ Phase 6: UI / UX

### Test 6.1 — ธีมสี
| เช็คอะไร | คาดหวัง |
|:--|:--|
| Background ทุกที่ | สี Deep Navy (#1A1A2E) ไม่ใช่ดำ |
| ปุ่ม START | Gradient ชมพู-ม่วง (#FF00CC→#7B2FFF) |
| ปุ่ม ghost ทุกตัว (Save, Clear, History...) | เม้าวาง → พื้นชมพูจาง + ขอบชมพู |
| ComboBox dropdown | เม้าวาง → item เปลี่ยนสีชมพู |
| Credit label | สีทอง (#FFD700) |

### Test 6.2 — ภาษา
| เช็คอะไร | คาดหวัง |
|:--|:--|
| หน้า Login/Register | ✅ ภาษาอังกฤษทั้งหมด |
| Error messages | ✅ ภาษาอังกฤษ |
| ทุก dialog | ✅ ภาษาอังกฤษ |
| ทุกปุ่ม/label | ✅ ไม่มีภาษาไทย |

### Test 6.3 — Layout
| เช็คอะไร | คาดหวัง |
|:--|:--|
| ย่อหน้าต่าง (1200×700) | ✅ ไม่มีอะไรหาย layout ไม่พัง |
| ขยายหน้าต่างเต็มจอ | ✅ Gallery ขยายตาม Sidebar/Inspector คงที่ |
| Title ใน Inspector | ✅ เห็น 2 บรรทัด อ่านง่าย |

### Test 6.4 — Re-export CSV
| ขั้นตอน | ทำอะไร | คาดหวัง |
|:-:|:--|:--|
| 1 | ทำ job สำเร็จ (มี CSV แล้ว) | — |
| 2 | คลิกไฟล์ที่สำเร็จ | Inspector แสดง metadata |
| 3 | แก้ Title เป็นอย่างอื่น | — |
| 4 | กด "🔄 Re-export CSV" | ✅ Dialog แสดง info + warning + checklist |
| 5 | กด "Re-export CSV" | ✅ CSV สร้างใหม่ มี title ที่แก้แล้ว |

**ผ่าน Phase 6:** ✅ ดูดี ใช้งานง่าย ภาษาถูก

---

## ทดสอบ Phase 7: จำลองหลาย User (เครื่องเดียว)

คุณไม่จำเป็นต้องมีหลายเครื่อง! ทำแบบนี้:

### วิธี: สร้างหลาย account บนเครื่องเดียว

| ขั้นตอน | ทำอะไร |
|:-:|:--|
| 1 | Login ด้วย user A (test@test.com) → ทำงานปกติ → Logout |
| 2 | สมัคร user B (test2@test.com) → ทำงานปกติ → Logout |
| 3 | Login กลับ user A → เช็คว่าเครดิตถูกต้อง ไม่ปนกัน |

### เช็ค:
- [ ] User A เครดิตไม่ลดเพราะ User B ใช้
- [ ] User B ไม่เห็นไฟล์ของ User A
- [ ] Login/Logout สลับได้ไม่มีปัญหา

### จำลอง Device Mismatch:
| ขั้นตอน | ทำอะไร | คาดหวัง |
|:-:|:--|:--|
| 1 | User A login สำเร็จ | — |
| 2 | ไปแก้ hardware_id ของ User A ใน Firestore เป็นค่าอื่น | — |
| 3 | Logout → Login ใหม่ | ✅ "Device mismatch" error |

---

## ทดสอบ Phase 8: Update System

### Test 8.1 — Optional Update
| ขั้นตอน | ทำอะไร | คาดหวัง |
|:-:|:--|:--|
| 1 | ใน Firestore: `system_config/app_settings` แก้ `latest_version: "2.0.1"` | — |
| 2 | เปิดโปรแกรม (version 2.0.0) | ✅ UpdateDialog: "Update Available" + Skip ได้ |
| 3 | กด Skip | ✅ เข้าใช้งานได้ปกติ |

### Test 8.2 — Force Update
| ขั้นตอน | ทำอะไร | คาดหวัง |
|:-:|:--|:--|
| 1 | แก้ `force_update_below: "2.1.0"` | — |
| 2 | เปิดโปรแกรม (version 2.0.0) | ✅ UpdateDialog: "Update Required" + ปิดไม่ได้ |

---

## ทดสอบ Phase 9: Performance (ทนทาน)

### Test 9.1 — ไฟล์จำนวนมาก
| ขั้นตอน | ทำอะไร | คาดหวัง |
|:-:|:--|:--|
| 1 | สร้างโฟลเดอร์มีรูป 50 ไฟล์ | — |
| 2 | เปิด folder | ✅ thumbnails โหลดทั้งหมด ไม่ crash |
| 3 | START (ต้องมีเครดิตพอ) | ✅ ทำได้ครบ ไม่ค้าง |
| 4 | เช็ค RAM (Activity Monitor / Task Manager) | ✅ ไม่เกิน 500 MB |

### Test 9.2 — วิดีโอใหญ่
| ขั้นตอน | ทำอะไร | คาดหวัง |
|:-:|:--|:--|
| 1 | เตรียมวิดีโอ 500MB 1 ไฟล์ | — |
| 2 | เปิด folder → START | ✅ สร้าง proxy → ประมวลผลได้ (อาจช้าแต่ไม่ crash) |

---

## สรุป: ลำดับการทดสอบ

```
วันที่ 1: Phase 1 (Auth) + Phase 2 (Credits)           ~1 ชั่วโมง
วันที่ 2: Phase 3 (AI Processing — Happy Path)          ~2 ชั่วโมง
วันที่ 3: Phase 4 (Error Handling)                       ~1.5 ชั่วโมง
วันที่ 4: Phase 5 (Crash Recovery) + Phase 6 (UI/UX)    ~1.5 ชั่วโมง
วันที่ 5: Phase 7 (Multi-User) + Phase 8 (Update)       ~1 ชั่วโมง
วันที่ 6: Phase 9 (Performance) + ทดสอบรวม               ~2 ชั่วโมง
```

**รวม ~9 ชั่วโมง แบ่ง 6 วัน วันละ 1.5 ชม.**

---

## เมื่อทดสอบเสร็จ: Checklist ก่อน Go-Live

```
AUTH:
☐ สมัครได้
☐ Login ได้
☐ Login ผิด → error message ชัดเจน
☐ Token saved → เปิดใหม่ไม่ต้อง login
☐ Device mismatch → blocked

CREDITS:
☐ ยอดเงินแสดงถูก
☐ Refresh ทำงาน
☐ TopUp dialog เปิดได้
☐ History เปิดได้

PROCESSING:
☐ iStock mode → CSV ถูกต้อง
☐ Adobe mode → CSV 2 ไฟล์ถูกต้อง
☐ Keyword quality ดี (ตรง dictionary, ไม่ซ้ำ)
☐ Title/Description ยาวถูกต้อง
☐ Video processing ทำงานได้

ERROR HANDLING:
☐ เครดิตไม่พอ → dialog ชัดเจน
☐ API Key ผิด → error + refund
☐ ไฟล์เสีย → error เฉพาะไฟล์ ไม่กระทบไฟล์อื่น
☐ Stop กลางทาง → refund ถูกต้อง
☐ Server ปิด → error message (ไม่ crash)

CRASH RECOVERY:
☐ Force quit → เปิดใหม่ → refund อัตโนมัติ

UI:
☐ ธีม Deep Navy ถูกต้อง
☐ ภาษาอังกฤษทั้งหมด
☐ Hover effects ทำงาน
☐ Re-export CSV ทำงาน

MULTI-USER:
☐ 2 accounts ไม่ปนกัน

ถ้า ☐ ทุกข้อเป็น ☑ → พร้อม Go-Live! 🚀
```

---

*คู่มือทดสอบ BigEye Pro — สำหรับ 1 คน 1 เครื่อง ไม่ต้องเขียนโค้ด*
